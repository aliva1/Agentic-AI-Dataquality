# Core
pandas>=2.0
SQLAlchemy>=2.0
numpy>=1.24

# Connectors (optional per-source; all listed so `pip install -r requirements.txt`
# gives you a fully working demo. In production you'd typically install only
# the extras you need — see README "Installing only what you need".)
psycopg2-binary>=2.9      # PostgreSQL
snowflake-sqlalchemy>=1.5 # Snowflake
databricks-sql-connector>=3.0  # Databricks
kafka-python>=2.0         # Kafka streaming source

# Knowledge / RAG
# Uses a self-contained hashing-vectorizer + cosine-similarity vector
# store (see dq_agent/knowledge/vector_store.py) so retrieval works
# fully offline with no model download. Swap in Chroma/pgvector/etc. by
# implementing the same VectorStore interface for production scale.
scikit-learn>=1.3

# LLM
anthropic>=0.34

# CLI
click>=8.1

# Testing
pytest>=8.0
pytest-cov>=5.0
